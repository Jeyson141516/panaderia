{
  "name": "panaderia-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions de Panadería El Vacán: API con CORS estricto y validación de datos en el servidor.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.0"
  }
}
